// different between display inline & display inline block?
// => if we are using inline then content in same line and inside inline block we are giving height and width and also.
// - the major different is that does not add a line break after the element.
