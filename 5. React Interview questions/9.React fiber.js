// React fiber is a reconcilliation search engine of react which is introduced in React 16.

// fiber mean that unit of work.

// fiber in a react is just a plain javascript object  with some properties.